# This is a fallback configuration

testModule {
  # enabled here, do don't need to enable explicitly in application.conf
  enabled = true

  # the following are default values and will be overriden in application.conf
  stringValue = ''
  booleanValue = false
  integerValue = 0

  listValue = [ A, B, C ]
  emptyListValue = [ ]
}